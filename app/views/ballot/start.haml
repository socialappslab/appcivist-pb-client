.container
  .row.text-center
    %h2
      -#%b> Assembly:
      -#PB Belleville Paris/
      %b> {{campaigns.length < 1 ? "" : "Campaign" }} Voting Ballot
      {{campaigns.length < 1 ? "" : " for " }} {{campaigns.length < 1 ? "" : "'"+campaigns[0].title+"'" }}
  #rangeLanding.jumbotron
    .titles
      .container
        %h3 {{campaigns.length < 1 ? "" : campaigns[0].title }}
      .container
        %h1
          %b Voting Ballot
      .container
        %h1#small {{ballot.starts_at | date:'yyyy'}} {{ballot.starts_at | date:'MM'}}
    %br
      .container.instructions
        %h2.title
          %strong Voting Instructions
          %button.btn.btn-success{"ng-click" => "transitionToRegistration()", :type => "button"} Register to Vote
        %ul
          %li{:type => "circle"} {{ballot.ballot.instructions}}
          %li{:type => "circle"}
            The voting system your Assembly selected and configured for this campaign is
            %b {{ballot.ballot.voting_system_type}}
          %li{:type => "none"}
            %ul
              %li{"ng-repeat" => "config in ballot.ballot_configurations", :type => "circle"} The {{config.key}} of this voting ballot is {{config.value}}.
          %li{:type => "circle"} When you are ready to vote, click the button "Start voting".
      %br
        .container.additional-info
          %h2
            %strong Additional Information
          %p.info{:style => "white-space: pre-wrap;"}{{ballot.notes}}
          %br
            %h3
              %strong Technical Issues
            %br
              %ul.list-unstyled
                %li Contact Cristhian at cristhian@example.com/ +1 999 999 9999
